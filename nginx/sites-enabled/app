server{
  listen 80;
  server_name mnpay.org;
  charset utf-8;
  location /static {
    alias /www/static;
  }
  location / {
    proxy_pass http://python:8000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forward-For $proxy_add_x_forwarded_for;
  }
}
